// EXIT:55
unit loopcall;

fn add(a: i32, b: i32) -> i32 { return a + b; }

fn main() -> i32 {
    var total: i32 = 0;
    var i: i32 = 0;
    while i < 10 {
        total = total + add(i, 1);
        i = i + 1;
    }
    return total;
}
