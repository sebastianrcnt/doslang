// EXIT:100
unit array;

fn main() -> i32 {
    let a: [4]i32 = [10, 20, 30, 40];
    var sum: i32 = 0;
    var i: i32 = 0;
    while i < 4 {
        sum = sum + a[i];
        i = i + 1;
    }
    return sum;
}
