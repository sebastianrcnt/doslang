// ERROR:11:drop may only be invoked by scope cleanup
unit bad_drop;

struct Box {
    value: i32,
    fn drop(self: &mut Self) { self.value = 0; }
}

fn bad() -> void {
    var b: Box = Box{ value: 1 };
    b.drop();
}
