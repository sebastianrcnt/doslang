// ERROR:8:cannot move a non-Copy value out of a projection; use mem.replace
unit bad_proj;

struct Holder { p: ^i32 }
fn take(p: ^i32) -> void { mem.destroy(p); }

fn bad(h: Holder) -> void {
    take(h.p);
}
