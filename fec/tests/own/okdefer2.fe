unit okdefer2;

fn read(r: &i32) -> i32 { return r.^; }

fn test() -> i32 {
    var x: i32 = 3;
    if true {
        let r = &x;
        defer { let y = read(r); }
    }
    x += 1;
    return x;
}

